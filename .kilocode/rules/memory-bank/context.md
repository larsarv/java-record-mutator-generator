# Context

The Java Record Mutator Generator (JRMG) project is currently in a stable state with core functionality implemented. The
project is set up as a Gradle multi-module project consisting of an API module, an annotation processor module, and an
example project.

## Current Work Focus

The current focus is on initializing and maintaining the Memory Bank documentation to ensure a comprehensive
understanding of the project for future development and maintenance.

## Recent Changes

- Initialized Memory Bank structure.
- Analyzed project structure and source code.
- Merged `CtorAnnotationProcessor` and `MtorAnnotationProcessor` into a single `AnnotationProcessor`.
- Extracted generation logic into `CtorGenerator` and `MtorGenerator`.

## Next Steps

- Continue with any pending feature requests or bug fixes (none currently specified).